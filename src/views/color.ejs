<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Home Page</title>
</head>
<body <%= locals.data ? 'style=background-color:' + data.color : '' %>>

	<% if(locals.data){ %>
		  <p>Hola <%= data.name %>, gracias por visitarnos.</p>
	<% } else { %>
		<p>Por favor, ingresa tus datos</p>
	<% } %>

        <button><a href="/">Volver al inicio</a></button>
</body>
</html>